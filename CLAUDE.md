# CLAUDE.md — 옥수수마켓 작업 규칙

이 폴더는 **개발 연습용** 프로젝트입니다. 작업하는 사람(사용자)은 비개발자이고,
공부를 목적으로 한 단계씩 만들어 갑니다. 아래 규칙을 항상 지켜 주세요.

---

## 1. 말하는 방식

- **개발 용어는 비개발자에게 맞게 쉽게 풀어서** 설명합니다.
  - 예: "서버 액션" → "버튼을 누르면 서버에서 대신 처리해 주는 함수"
  - 영어 용어를 쓸 때는 괄호로 한 줄 설명을 붙입니다. 예: `RLS(행 단위 보안 — 내 데이터만 보이게 하는 규칙)`
- **지금 무엇을 하고 있는지 자주 말합니다.**
  - 파일을 만들기 전 / 명령을 실행하기 전에 한 줄로 "지금 ○○ 합니다"라고 알립니다.
  - 여러 단계를 한 번에 몰아서 하지 말고, 중간중간 진행 상황을 보고합니다.
- 결과 보고는 **무엇이 달라졌는지 + 화면에서 어떻게 확인하는지** 순서로 씁니다.
- 실패했으면 실패했다고 그대로 말합니다. 얼버무리지 않습니다.

## 2. 작업 범위

- **사용자가 지시한 것만** 합니다. 시키지 않은 기능·리팩터링·파일 정리는 하지 않습니다.
- 하나의 요청은 **한 단계씩** 끝냅니다. 다음 단계가 보이더라도 먼저 제안만 하고, "해 줘"라고 할 때 진행합니다.
- 하다가 더 좋은 방법이 보이면 **먼저 한 줄로 알리고** 사용자의 선택을 받습니다.

## 3. 사람이 해야 하는 일 안내

- 코드 작성, 파일 수정, 터미널 명령, DB 작업 등 **할 수 있는 것은 최대한 직접** 합니다.
- 사람만 할 수 있는 일(웹사이트 로그인이 필요한 설정, 카드 결제, 비밀번호 입력 등)만 넘깁니다.
- 넘길 때는 이런 형식으로 **번호를 매겨** 안내합니다.

  ```
  🙋 여기는 직접 해 주세요
  1) 브라우저에서 https://supabase.com/dashboard 접속
  2) 왼쪽 메뉴 Authentication → URL Configuration 클릭
  3) Site URL 칸에 배포 주소 붙여넣기 → Save
  끝나면 "했어"라고 알려 주세요.
  ```

- 무엇을, 어디서, 어떤 버튼을 누르는지까지 구체적으로 씁니다.

## 4. 개발이 끝나면 바로 확인시켜 주기

한 단계 작업이 끝나면 **말로 설명만 하지 말고 화면을 띄워서** 보여 줍니다.

1. `preview_start` 로 `.claude/launch.json` 의 `oksusu-market` 개발 서버를 켭니다. (`npm run dev` 를 터미널로 직접 실행하지 않습니다)
2. 브라우저 창에서 방금 만든 페이지를 엽니다.
3. 콘솔 오류 / 화면을 직접 확인하고, 문제가 있으면 고친 뒤 다시 확인합니다.
4. 사용자에게 "여기 열렸습니다, 이렇게 눌러 보세요"라고 안내합니다.

사용자에게 "직접 확인해 보세요"라고 떠넘기지 않습니다.

---

## 5. 이 프로젝트 기본 정보

| 항목 | 내용 |
| --- | --- |
| 이름 | 옥수수마켓 🌽 (당근마켓 스타일 중고거래 연습용) |
| 프레임워크 | Next.js 15 (App Router, 서버 컴포넌트 + 서버 액션) |
| 스타일 | Tailwind CSS v4 (`src/app/globals.css` 의 `@theme` 에 옥수수 색 팔레트) |
| 백엔드/DB | Supabase (로그인 + Postgres) |
| 언어 | TypeScript |
| 배포 | GitHub 에 push → **Vercel** 이 자동으로 빌드·배포 |

### 폴더 지도

```
src/
├─ middleware.ts              모든 요청에서 로그인 상태 갱신 + 접근 제어
├─ lib/supabase/              Supabase 연결 (client=브라우저, server=서버, middleware=토큰갱신)
├─ lib/validation.ts          입력값 검사 + 에러 메시지 한국어 번역
├─ app/auth/actions.ts        회원가입 / 로그인 / 로그아웃 처리
├─ app/(각 페이지)/page.tsx     화면
└─ components/                마스코트 · 헤더 · 폼 · 공통 UI
```

자세한 내용(테이블 구조, 지금까지 만든 기능, 다음 단계)은 [README.md](README.md) 에 있습니다.
**기능을 하나 완성할 때마다 README.md 도 같이 업데이트**해 주세요.

---

## 6. 개발 환경에서 주의할 점 (Windows)

- 이 PC는 `node` / `npm` 이 PATH 에 안 잡혀 있을 수 있습니다. 실패하면 전체 경로를 씁니다:
  ```
  "C:/Program Files/nodejs/npm.cmd" install
  ```
- 개발 서버는 `preview_start` 로 띄웁니다. 백그라운드 Bash 로 띄우지 않습니다.
- `.env.local` 은 **절대 git 에 올리지 않습니다** (이미 `.gitignore` 에 있음).
  새 환경 변수를 추가하면 `.env.example` 에 "값 없이 이름만" 같이 추가합니다.

## 7. Supabase 사용 규칙

- 이 Supabase 프로젝트는 **가계부 프로젝트와 공유**합니다. 테이블만 서로 다릅니다.
  - 가계부 → `public.entries` → **건드리지 않습니다**
  - 옥수수마켓 → `public.profiles`, 앞으로 `products` 등
- 스키마(테이블 구조)를 바꾸기 전에 **먼저 현재 테이블을 확인**하고, 무엇을 바꿀지 사용자에게 한 줄로 알린 뒤 진행합니다.
- 삭제(`drop`, `delete`)가 들어가는 SQL 은 **실행 전에 반드시 사용자 확인**을 받습니다.
- 새 테이블을 만들면 RLS(행 단위 보안)를 켜고 정책까지 같이 만듭니다.

## 8. Git / 배포 규칙

- 커밋과 push 는 **사용자가 "올려 줘"라고 할 때만** 합니다. 임의로 하지 않습니다.
- 커밋 메시지는 한국어로, 무엇을 했는지 한 줄. 예: `상품 등록 페이지 추가`
- 배포 흐름: `main` 브랜치에 push → Vercel 이 자동 빌드 → 배포 주소로 반영.
- push 하기 전에 `npm run build` 로 빌드가 통과하는지 먼저 확인합니다. (Vercel 에서 깨지면 원인 찾기가 어렵기 때문)
- 배포 후 사용자에게 안내할 것:
  - Vercel 프로젝트 설정에 `.env.local` 과 같은 환경 변수 3개 넣기
  - `NEXT_PUBLIC_SITE_URL` 은 배포 주소로 바꾸기
  - Supabase → Authentication → URL Configuration 에 배포 주소와 `/auth/callback`, `/auth/confirm` 등록

## 9. 코드 스타일

- 주변 코드와 같은 모양으로 씁니다 (같은 들여쓰기, 같은 이름 짓기 방식).
- 새로 만드는 컴포넌트는 `src/components/` 에, 재사용 로직은 `src/lib/` 에 둡니다.
- 화면에 보이는 글자는 **한국어**로 씁니다.
- 어려운 부분에만 한국어 주석을 답니다. 줄마다 주석 달지 않습니다.
